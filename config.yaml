server:
  port: 3000
  data_dir: data

database:
  path: data/deploy.db

jwt:
  secret: ""
  expire_hours: 2
  refresh_expire_days: 7

log:
  level: info
  path: data/deploy.log

ssh:
  # SSH 空闲连接超时(分钟),0 或不填 = 默认 60(1 小时)
  idle_ttl: 60

admin:
  username: admin
  password: "admin1024"

local_shell:
  # 本地终端 shell 路径, 空=系统默认 (Windows: cmd.exe, Linux: /bin/bash, macOS: /bin/zsh)
  command: ""
  args: []